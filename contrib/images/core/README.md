# core

The `core` toolbox-vim image.

Currently, being published as `docker.io/mattjmcnaughton/toolbox-vim:$TAG`.

We will almost certainly want to come up with a more descriptive name/strategy for
how many images we should have, etc.
